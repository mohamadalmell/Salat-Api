<form method="post" action="{{ path('app_khateeb_delete', {'id': khateeb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ khateeb.id) }}">
    <button class="btn">Delete</button>
</form>
