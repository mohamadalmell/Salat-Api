{% extends 'base.html.twig' %}

{% block title %}Khateeb{% endblock %}

{% block body %}
    <h1>Khateeb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ khateeb.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ khateeb.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_khateeb_index') }}">back to list</a>

    <a href="{{ path('app_khateeb_edit', {'id': khateeb.id}) }}">edit</a>

    {{ include('khateeb/_delete_form.html.twig') }}
{% endblock %}
